# jshERP 安全问题检查与修复报告

## 文档信息
- 检查日期：2025年
- 检查范围：jshERP-boot（后端Java代码）、jshERP-web（前端Vue代码）
- 检查重点：第三方依赖漏洞、XSS/CSRF/SQL注入等网络攻击、敏感信息泄露、不安全加密行为

---

## 一、已修复的安全问题

### 1. 后端依赖漏洞（高/中风险）

#### 1.1 fastjson 反序列化漏洞（高风险）
- **问题描述**：fastjson 1.2.83 版本存在已知的反序列化漏洞，攻击者可通过恶意JSON数据执行任意代码。
- **原版本**：1.2.83
- **修复后版本**：2.0.43
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：升级到最新安全版本

#### 1.2 Log4j2 Log4Shell 漏洞（高风险）
- **问题描述**：log4j-to-slf4j 2.15.0 版本存在Log4Shell（CVE-2021-44228）漏洞，可导致远程代码执行。
- **原版本**：2.15.0
- **修复后版本**：2.23.1
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：升级到安全版本

#### 1.3 Apache HttpClient 版本过老（中风险）
- **问题描述**：httpclient 4.5.2 版本较老，存在多个已知安全漏洞。
- **原版本**：4.5.2
- **修复后版本**：4.5.14
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：升级到最新稳定版本

#### 1.4 commons-io 版本过老且groupId错误（中风险）
- **问题描述**：commons-io 1.3.2 版本极其老旧，且groupId配置错误（应为org.apache.commons）。
- **原版本**：1.3.2（groupId: commons-io）
- **修复后版本**：2.15.1（groupId: org.apache.commons）
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：修正groupId并升级到安全版本

#### 1.5 spring-boot-starter-redis 版本过老（中风险）
- **问题描述**：spring-boot-starter-redis 1.4.1.RELEASE 是Spring Boot 1.x的废弃starter，已不再维护。
- **原依赖**：spring-boot-starter-redis 1.4.1.RELEASE
- **修复后依赖**：spring-boot-starter-data-redis（Spring Boot 2.x标准starter）
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：替换为Spring Boot 2.x标准Redis starter

---

### 2. 数据库连接安全问题（高风险）

#### 2.1 allowMultiQueries 导致SQL注入风险（高风险）
- **问题描述**：数据库连接URL中配置了`allowMultiQueries=true`，允许执行多条SQL语句，大幅增加SQL注入攻击风险。
- **原配置**：`jdbc:mysql://127.0.0.1:3306/jsh_erp?useUnicode=true&characterEncoding=utf8&useCursorFetch=true&defaultFetchSize=500&rewriteBatchedStatements=true&useSSL=false&allowMultiQueries=true`
- **修复后配置**：`jdbc:mysql://127.0.0.1:3306/jsh_erp?useUnicode=true&characterEncoding=utf8&useCursorFetch=true&defaultFetchSize=500&rewriteBatchedStatements=true&useSSL=false&serverTimezone=Asia/Shanghai`
- **修改文件**：`jshERP-boot/src/main/resources/application.properties`
- **修复方式**：移除`allowMultiQueries=true`，添加时区配置

---

### 3. 安全HTTP响应头配置（中风险）

#### 3.1 缺少安全HTTP响应头
- **问题描述**：系统未配置安全HTTP响应头，容易受到XSS、Clickjacking等攻击。
- **修复方式**：创建`SecurityConfig.java`配置类，添加安全响应头过滤器
- **修改文件**：`jshERP-boot/src/main/java/com/jsh/erp/config/SecurityConfig.java`（新建）
- **添加的响应头**：
  - `X-Content-Type-Options: nosniff` - 防止MIME类型嗅探
  - `X-XSS-Protection: 1; mode=block` - 启用浏览器XSS保护
  - `X-Frame-Options: SAMEORIGIN` - 防止Clickjacking攻击
  - `Referrer-Policy: strict-origin-when-cross-origin` - 控制Referrer信息泄露

---

### 4. 前端依赖漏洞（高/中风险）

#### 4.1 jQuery XSS漏洞（高风险）
- **问题描述**：jQuery 1.12.4 版本存在多个已知XSS漏洞（如CVE-2015-9251、CVE-2019-11358等）。
- **原版本**：^1.12.4
- **修复后版本**：^3.7.1
- **修改文件**：`jshERP-web/package.json`
- **修复方式**：升级到最新安全版本

#### 4.2 axios 版本过老（中风险）
- **问题描述**：axios 0.18.0 版本较老，存在多个已知安全漏洞。
- **原版本**：^0.18.0
- **修复后版本**：^0.28.1
- **修改文件**：`jshERP-web/package.json`
- **修复方式**：升级到安全版本

---

### 5. 不安全的随机数生成（中风险）

#### 5.1 使用非密码学安全的随机数生成器
- **问题描述**：`Tools.java`中使用`Math.random()`和`new Random()`生成随机数，这些方法不是密码学安全的，可被预测。
- **原代码**：
  ```java
  int rand = (int) Math.round(Math.random() * 1);
  Random random = new Random();
  ```
- **修复后代码**：
  ```java
  private static final SecureRandom secureRandom = new SecureRandom();
  int rand = secureRandom.nextInt(3);
  ```
- **修改文件**：`jshERP-boot/src/main/java/com/jsh/erp/utils/Tools.java`
- **修复方式**：使用`java.security.SecureRandom`替代不安全的随机数生成方法
- **影响方法**：
  - `getRandomChar()` - 生成随机字符串
  - `getCharAndNum(int length)` - 生成指定长度的随机数字字母组合

---

## 二、遗留安全问题（需后续处理）

### 1. SQL注入风险（高风险）

#### 1.1 QueryUtils.filter() 方法SQL注入（高风险）
- **问题位置**：`jshERP-boot/src/main/java/com/jsh/erp/utils/QueryUtils.java:93-141`
- **问题描述**：`filter()`方法直接将用户输入的`name`和`value`拼接到SQL语句中，未进行参数化处理或充分过滤。
- **风险代码片段**：
  ```java
  String key = object.getString("name");
  builder.append("`").append(key).append("`");  // 直接拼接列名
  builder.append(value.getString(vidx));  // 直接拼接值到IN子句
  ```
- **安全等级**：🔴 高风险
- **建议修复方案**：
  1. 对列名使用白名单验证
  2. 对值使用参数化查询（PreparedStatement）
  3. 加强`filterSqlSpecialChar()`方法的过滤逻辑

#### 1.2 OrderUtils 排序SQL注入（高风险）
- **问题位置**：`jshERP-boot/src/main/java/com/jsh/erp/utils/OrderUtils.java`
- **问题描述**：`getOrderString()`和`getJoinTablesOrderString()`方法直接拼接用户输入的列名和排序方式到SQL语句中。
- **风险代码片段**：
  ```java
  String column = ColumnPropertyUtil.propertyToColumn(splits[0]);  // 仅做格式转换，无验证
  return "convert(" + column + " using gbk) " + splits[1];  // 直接拼接排序方式
  ```
- **安全等级**：🔴 高风险
- **建议修复方案**：
  1. 对列名使用白名单验证
  2. 对排序方式（asc/desc）进行严格验证

#### 1.3 MyBatis XML中${}语法SQL注入风险（中风险）
- **问题位置**：`jshERP-boot/src/main/resources/mapper_xml/*.xml`
- **问题描述**：大量Mapper XML文件中使用`${orderByClause}`、`${criterion.condition}`等字符串替换语法，存在SQL注入风险。
- **风险示例**：
  ```xml
  order by ${orderByClause}
  and ${criterion.condition}
  ```
- **安全等级**：🟡 中风险
- **说明**：这是MyBatis Generator生成的标准代码，`${}`用于需要动态SQL片段的场景。但需要确保调用方对输入进行严格验证。
- **建议修复方案**：
  1. 对`orderByClause`使用白名单验证
  2. 审查所有使用`${}`的地方，确保输入可控

---

### 2. 密码加密安全问题（高风险）

#### 2.1 使用MD5进行密码加密（高风险）
- **问题位置**：
  - 后端：`jshERP-boot/src/main/java/com/jsh/erp/utils/Tools.java:582-588`
  - 前端：`jshERP-web/src/views/user/Login.vue`、`Register.vue`等
- **问题描述**：前后端都使用MD5算法进行密码加密。MD5已被破解，存在彩虹表攻击风险。
- **风险代码片段**：
  ```java
  // 后端
  MessageDigest md = MessageDigest.getInstance("MD5");
  // 前端
  import md5 from 'md5'
  password: md5(this.passwordForm.password)
  ```
- **安全等级**：🔴 高风险
- **影响范围**：
  - 用户登录密码加密
  - 用户注册密码加密
  - 用户密码修改
  - 租户默认密码生成
- **建议修复方案**：
  1. 使用bcrypt、Argon2或PBKDF2等强哈希算法
  2. 添加盐值（Salt）
  3. 前后端同步修改，确保兼容性（可能需要迁移现有用户密码）
- **注意**：此修改涉及前后端协议变更，需要谨慎处理，建议制定详细的迁移计划。

---

### 3. 敏感信息配置问题（中风险）

#### 3.1 配置文件明文存储数据库密码（中风险）
- **问题位置**：`jshERP-boot/src/main/resources/application.properties:10`
- **问题描述**：数据库密码以明文形式存储在配置文件中。
- **风险配置**：
  ```properties
  spring.datasource.password=123456
  ```
- **安全等级**：🟡 中风险
- **建议修复方案**：
  1. 使用环境变量（如`${DB_PASSWORD}`）
  2. 使用配置中心（如Nacos、Apollo）
  3. 使用Jasypt等配置加密工具

#### 3.2 默认密码过于简单（中风险）
- **问题位置**：`jshERP-boot/src/main/java/com/jsh/erp/constants/BusinessConstants.java:160`
- **问题描述**：新增用户默认密码为"123456"，过于简单。
- **风险代码**：
  ```java
  public static final String USER_DEFAULT_PASSWORD = "123456";
  ```
- **安全等级**：🟡 中风险
- **建议修复方案**：
  1. 使用随机生成的强密码
  2. 强制用户首次登录修改密码
  3. 或至少使用更复杂的默认密码

---

### 4. 前端安全问题（中风险）

#### 4.1 lodash.get 和 lodash.pick 原型污染风险（中风险）
- **问题位置**：`jshERP-web/package.json:22-23`
- **问题描述**：`lodash.get` 4.4.2 和 `lodash.pick` 4.4.0 版本存在原型污染漏洞（CVE-2020-28500、CVE-2021-23337等）。
- **风险依赖**：
  ```json
  "lodash.get": "^4.4.2",
  "lodash.pick": "^4.4.0"
  ```
- **安全等级**：🟡 中风险
- **建议修复方案**：
  1. 升级到完整版lodash 4.17.21+ 或 lodash-es
  2. 或使用更安全的替代方案
  3. 审查代码中是否存在用户可控的路径访问

---

### 5. CSRF防护缺失（中风险）

#### 5.1 缺少CSRF Token验证机制（中风险）
- **问题描述**：系统虽然有基于Token的身份验证，但未实现CSRF Token验证机制。
- **当前实现**：使用JWT Token进行身份验证，存储在请求头中。
- **安全等级**：🟡 中风险
- **风险说明**：
  - 如果Token存储在localStorage，CSRF风险较低（需配合XSS）
  - 如果Token存储在Cookie且未设置SameSite属性，则存在CSRF风险
- **建议修复方案**：
  1. 检查Cookie的SameSite属性设置
  2. 考虑实施CSRF Token验证（Double Submit Cookie模式）
  3. 确保关键操作需要二次验证

---

## 三、安全修复汇总

### 已完成修复统计
| 类别 | 高风险 | 中风险 | 低风险 | 总计 |
|------|--------|--------|--------|------|
| 依赖漏洞 | 2 | 4 | 0 | 6 |
| 配置问题 | 1 | 1 | 0 | 2 |
| 随机数安全 | 0 | 1 | 0 | 1 |
| HTTP安全头 | 0 | 1 | 0 | 1 |
| **总计** | **3** | **7** | **0** | **10** |

### 遗留问题统计
| 类别 | 高风险 | 中风险 | 低风险 | 总计 |
|------|--------|--------|--------|------|
| SQL注入 | 2 | 1 | 0 | 3 |
| 密码加密 | 1 | 0 | 0 | 1 |
| 敏感信息 | 0 | 2 | 0 | 2 |
| 前端依赖 | 0 | 1 | 0 | 1 |
| CSRF防护 | 0 | 1 | 0 | 1 |
| **总计** | **3** | **5** | **0** | **8** |

---

## 四、修复建议优先级

### 紧急修复（P0）
1. QueryUtils.filter() SQL注入漏洞
2. OrderUtils 排序SQL注入漏洞
3. MD5密码加密升级

### 高优先级（P1）
1. 配置文件敏感信息加密
2. 前端lodash原型污染风险
3. 默认密码强度提升

### 中优先级（P2）
1. CSRF Token验证机制
2. MyBatis ${}语法输入验证
3. 安全审计日志增强

---

## 五、安全最佳实践建议

### 开发阶段
1. **依赖管理**：定期使用OWASP Dependency-Check、Snyk等工具扫描依赖漏洞
2. **代码审查**：建立安全代码审查流程，重点关注SQL拼接、加密算法、输入验证
3. **安全测试**：集成SAST（静态应用安全测试）工具到CI/CD流程

### 部署阶段
1. **配置安全**：使用环境变量或配置中心管理敏感配置，避免明文存储
2. **HTTPS**：生产环境强制使用HTTPS，配置HSTS响应头
3. **WAF**：考虑部署Web应用防火墙

### 运维阶段
1. **日志审计**：完善安全日志，记录关键操作和异常访问
2. **定期扫描**：定期进行漏洞扫描和渗透测试
3. **应急响应**：建立安全事件应急响应流程

---

## 六、修改文件清单

### 已修改文件
1. `jshERP-boot/pom.xml` - 依赖版本升级
2. `jshERP-boot/src/main/resources/application.properties` - 数据库连接配置
3. `jshERP-boot/src/main/java/com/jsh/erp/config/SecurityConfig.java` - 新建安全配置
4. `jshERP-boot/src/main/java/com/jsh/erp/utils/Tools.java` - 随机数生成器升级
5. `jshERP-web/package.json` - 前端依赖版本升级

### 需修改文件（遗留问题）
1. `jshERP-boot/src/main/java/com/jsh/erp/utils/QueryUtils.java` - SQL注入修复
2. `jshERP-boot/src/main/java/com/jsh/erp/utils/OrderUtils.java` - SQL注入修复
3. `jshERP-boot/src/main/java/com/jsh/erp/utils/Tools.java` - MD5加密升级
4. `jshERP-boot/src/main/java/com/jsh/erp/constants/BusinessConstants.java` - 默认密码
5. `jshERP-web/package.json` - lodash依赖升级
6. `jshERP-web/src/views/user/*.vue` - 密码加密逻辑

---

**文档编写完成时间**：2025年
**最后更新时间**：2025年
**检查人**：安全审计团队
