# jshERP 安全问题检查与修复报告

## 文档信息
- 检查范围：jshERP-boot（后端Java代码）、jshERP-web（前端Vue代码）
- 检查重点：第三方依赖漏洞、XSS/CSRF/SQL注入等网络攻击、敏感信息泄露、不安全加密行为

---

## 一、已修复的安全问题

### 1. 后端依赖漏洞

#### 1.1 fastjson 反序列化漏洞
- **问题描述**：fastjson 1.2.83 版本存在已知的反序列化漏洞，攻击者可通过恶意JSON数据执行任意代码。
- **原版本**：1.2.83
- **修复后版本**：2.0.43
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：升级到最新安全版本

#### 1.2 Log4j2 Log4Shell 漏洞
- **问题描述**：log4j-to-slf4j 2.15.0 版本存在Log4Shell（CVE-2021-44228）漏洞，可导致远程代码执行。
- **原版本**：2.15.0
- **修复后版本**：2.23.1
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：升级到安全版本

#### 1.3 Apache HttpClient 版本过老
- **问题描述**：httpclient 4.5.2 版本较老，存在多个已知安全漏洞。
- **原版本**：4.5.2
- **修复后版本**：4.5.14
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：升级到最新稳定版本

#### 1.4 commons-io 版本过老且groupId错误
- **问题描述**：commons-io 1.3.2 版本极其老旧，且groupId配置错误（应为org.apache.commons）。
- **原版本**：1.3.2（groupId: commons-io）
- **修复后版本**：2.15.1（groupId: org.apache.commons）
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：修正groupId并升级到安全版本

#### 1.5 spring-boot-starter-redis 版本过老
- **问题描述**：spring-boot-starter-redis 1.4.1.RELEASE 是Spring Boot 1.x的废弃starter，已不再维护。
- **原依赖**：spring-boot-starter-redis 1.4.1.RELEASE
- **修复后依赖**：spring-boot-starter-data-redis（Spring Boot 2.x标准starter）
- **修改文件**：`jshERP-boot/pom.xml`
- **修复方式**：替换为Spring Boot 2.x标准Redis starter

---

### 2. 数据库连接安全问题

#### 2.1 allowMultiQueries 导致SQL注入风险
- **问题描述**：数据库连接URL中配置了`allowMultiQueries=true`，允许执行多条SQL语句，大幅增加SQL注入攻击风险。
- **原配置**：`jdbc:mysql://127.0.0.1:3306/jsh_erp?useUnicode=true&characterEncoding=utf8&useCursorFetch=true&defaultFetchSize=500&rewriteBatchedStatements=true&useSSL=false&allowMultiQueries=true`
- **修复后配置**：`jdbc:mysql://127.0.0.1:3306/jsh_erp?useUnicode=true&characterEncoding=utf8&useCursorFetch=true&defaultFetchSize=500&rewriteBatchedStatements=true&useSSL=false&serverTimezone=Asia/Shanghai`
- **修改文件**：`jshERP-boot/src/main/resources/application.properties`
- **修复方式**：移除`allowMultiQueries=true`，添加时区配置

---

### 3. 安全HTTP响应头配置

#### 3.1 缺少安全HTTP响应头
- **问题描述**：系统未配置安全HTTP响应头，容易受到XSS、Clickjacking等攻击。
- **修复方式**：创建`SecurityConfig.java`配置类，添加安全响应头过滤器
- **修改文件**：`jshERP-boot/src/main/java/com/jsh/erp/config/SecurityConfig.java`（新建）
- **添加的响应头**：
  - `X-Content-Type-Options: nosniff` - 防止MIME类型嗅探
  - `X-XSS-Protection: 1; mode=block` - 启用浏览器XSS保护
  - `X-Frame-Options: SAMEORIGIN` - 防止Clickjacking攻击
  - `Referrer-Policy: strict-origin-when-cross-origin` - 控制Referrer信息泄露

---

### 4. 前端依赖漏洞

#### 4.1 jQuery XSS漏洞
- **问题描述**：jQuery 1.12.4 版本存在多个已知XSS漏洞（如CVE-2015-9251、CVE-2019-11358等）。
- **原版本**：^1.12.4
- **修复后版本**：^3.7.1
- **修改文件**：`jshERP-web/package.json`
- **修复方式**：升级到最新安全版本

#### 4.2 axios 版本过老
- **问题描述**：axios 0.18.0 版本较老，存在多个已知安全漏洞。
- **原版本**：^0.18.0
- **修复后版本**：^0.28.1
- **修改文件**：`jshERP-web/package.json`
- **修复方式**：升级到安全版本

---

### 5. 不安全的随机数生成

#### 5.1 使用非密码学安全的随机数生成器
- **问题描述**：`Tools.java`中使用`Math.random()`和`new Random()`生成随机数，这些方法不是密码学安全的，可被预测。
- **原代码**：
  ```java
  int rand = (int) Math.round(Math.random() * 1);
  Random random = new Random();
  ```
- **修复后代码**：
  ```java
  private static final SecureRandom secureRandom = new SecureRandom();
  int rand = secureRandom.nextInt(3);
  ```
- **修改文件**：`jshERP-boot/src/main/java/com/jsh/erp/utils/Tools.java`
- **修复方式**：使用`java.security.SecureRandom`替代不安全的随机数生成方法
- **影响方法**：
  - `getRandomChar()` - 生成随机字符串
  - `getCharAndNum(int length)` - 生成指定长度的随机数字字母组合

---

### 6. SQL注入风险（新增修复）

#### 6.1 QueryUtils.filter() 方法SQL注入
- **问题位置**：`jshERP-boot/src/main/java/com/jsh/erp/utils/QueryUtils.java`
- **问题描述**：`filter()`方法直接将用户输入的`name`（列名）和`value`（值）拼接到SQL语句中，存在SQL注入风险。
- **修复方式**：
  1. 添加列名验证：使用正则表达式`^[a-zA-Z_][a-zA-Z0-9_]*$`验证列名
  2. 添加值转义：对字符串值添加单引号并转义内部单引号，数字值直接使用
  3. 无效列名直接跳过，返回空字符串
- **修改文件**：`jshERP-boot/src/main/java/com/jsh/erp/utils/QueryUtils.java`
- **新增验证方法**：
  - `isValidColumnName(String column)` - 验证列名合法性
  - `escapeSqlValue(String value)` - 转义SQL值

#### 6.2 OrderUtils 排序SQL注入
- **问题位置**：`jshERP-boot/src/main/java/com/jsh/erp/utils/OrderUtils.java`
- **问题描述**：`getOrderString()`和`getJoinTablesOrderString()`方法直接拼接用户输入的列名和排序方式到SQL语句中，存在SQL注入风险。
- **修复方式**：
  1. 添加列名验证：使用正则表达式`^[a-zA-Z_][a-zA-Z0-9_]*$`验证列名
  2. 添加排序方式验证：只允许`ASC`或`DESC`（不区分大小写）
  3. 添加表名验证：`getJoinTablesOrderString()`方法验证表名
  4. 验证失败返回空字符串，不进行拼接
- **修改文件**：`jshERP-boot/src/main/java/com/jsh/erp/utils/OrderUtils.java`
- **新增验证方法**：
  - `isValidColumnName(String column)` - 验证列名合法性
  - `isValidTableName(String table)` - 验证表名合法性
  - `isValidOrderDirection(String direction)` - 验证排序方向（只能是ASC/DESC）

---

## 二、遗留安全问题（需后续处理）

### 1. 密码加密安全问题（重点关注：不安全加密行为）

#### 1.1 使用MD5进行密码加密
- **问题位置**：
  - 后端：`jshERP-boot/src/main/java/com/jsh/erp/utils/Tools.java`
  - 前端：`jshERP-web/src/views/user/Login.vue`、`Register.vue`等
- **问题描述**：前后端都使用MD5算法进行密码加密。MD5已被破解，存在彩虹表攻击风险。
- **安全等级**：🔴 高风险
- **无法立即修复的原因**：
  1. **前后端协议绑定**：前端先进行MD5加密，后端接收后直接与数据库存储的MD5哈希比对。修改算法需要前后端同步更改。
  2. **现有用户密码迁移**：数据库中已存储大量用户的MD5密码哈希。如果直接更换算法，现有用户将无法登录。
  3. **需要双重验证过渡期**：安全的迁移方案需要支持两种算法并存（优先使用新算法验证，失败时尝试旧算法），逐步迁移用户密码。
  4. **影响范围大**：涉及用户登录、注册、密码重置、租户创建等多个核心流程。
- **影响范围**：
  - 用户登录密码加密
  - 用户注册密码加密
  - 用户密码修改
  - 租户默认密码生成
- **建议修复方案**：
  1. 使用bcrypt、Argon2或PBKDF2等强哈希算法
  2. 添加盐值（Salt）
  3. 制定详细的迁移计划，支持双重验证过渡期
  4. 前后端同步修改

---

### 2. 敏感信息配置问题（重点关注：敏感信息泄露）

#### 2.1 配置文件明文存储数据库密码
- **问题位置**：`jshERP-boot/src/main/resources/application.properties`
- **问题描述**：数据库密码以明文形式存储在配置文件中。
- **安全等级**：🟡 中风险
- **无法立即修复的原因**：
  1. **部署环境问题**：这是部署环境的配置问题，不是代码逻辑问题。代码层面无法决定部署时使用何种配置方式。
  2. **需要运维配合**：使用环境变量、配置中心或加密工具需要运维人员在部署时进行配置。
  3. **开发环境便利性**：开发环境通常使用明文配置以便于调试，生产环境应使用更安全的方式。
- **建议修复方案**：
  1. 使用环境变量（如`${DB_PASSWORD}`）
  2. 使用配置中心（如Nacos、Apollo）
  3. 使用Jasypt等配置加密工具

#### 2.2 默认密码过于简单
- **问题位置**：`jshERP-boot/src/main/java/com/jsh/erp/constants/BusinessConstants.java`
- **问题描述**：新增用户默认密码为"123456"，过于简单。
- **安全等级**：🟡 中风险
- **无法立即修复的原因**：
  1. **前后端多处硬编码**：前端多个文件（`UserModal.vue`、`TenantModal.vue`、`UserList.vue`等）中硬编码了"123456"作为提示文字或默认密码。
  2. **用户体验考虑**：系统已有"密码过于简单"的检测逻辑（登录时检查密码是否为默认密码并提示用户修改）。
  3. **功能一致性要求**：修改默认密码需要确保前后端、数据库初始化脚本等所有地方保持一致。
- **建议修复方案**：
  1. 使用随机生成的强密码
  2. 强制用户首次登录修改密码
  3. 或至少使用更复杂的默认密码

---

### 3. 前端依赖问题（重点关注：依赖漏洞）

#### 3.1 lodash.get 和 lodash.pick 原型污染风险
- **问题位置**：`jshERP-web/package.json`
- **问题描述**：`lodash.get` 4.4.2 和 `lodash.pick` 4.4.0 是独立的功能子包，版本过老且不再维护。
- **安全等级**：🟡 中风险
- **无法立即修复的原因**：
  1. **涉及文件数量大**：项目中有约60个文件使用`import pick from 'lodash.pick'`或`import get from 'lodash.get'`。
  2. **替换成本高**：替换为完整版lodash需要修改所有import语句，并可能存在兼容性问题。
  3. **实际风险较低**：这些独立子包只包含读取/选择属性的功能，不包含完整版lodash中会导致原型污染的方法（如`set`、`merge`等）。
- **建议修复方案**：
  1. 升级到完整版lodash 4.17.21+ 或 lodash-es
  2. 或使用更安全的替代方案
  3. 逐步迁移，分批修改

---

### 4. CSRF防护问题（重点关注：网络攻击）

#### 4.1 CSRF风险评估
- **问题描述**：系统是否存在CSRF风险。
- **安全等级**：🟡 低风险
- **无法立即修复的原因**：
  1. **Token存储方式安全**：系统使用JWT Token存储在localStorage中，通过自定义请求头`X-Access-Token`传递。
  2. **浏览器不会自动携带**：CSRF攻击利用浏览器自动携带Cookie的特性。由于Token存储在localStorage且通过自定义请求头传递，浏览器不会在跨域请求中自动携带。
  3. **现有机制已提供保护**：基于Token的认证方式本身就具有一定的CSRF防护能力。
- **建议优化方案**：
  1. 检查Cookie的SameSite属性设置（如果有使用Cookie）
  2. 关键操作考虑添加二次验证

---

### 5. MyBatis XML动态SQL问题

#### 5.1 MyBatis XML中${}语法SQL注入风险
- **问题位置**：`jshERP-boot/src/main/resources/mapper_xml/*.xml`
- **问题描述**：大量Mapper XML文件中使用`${orderByClause}`、`${criterion.condition}`等字符串替换语法。
- **安全等级**：🟡 中风险
- **无法立即修复的原因**：
  1. **MyBatis Generator生成代码**：这是MyBatis Generator自动生成的标准代码，`${}`语法用于需要动态SQL片段的场景（如动态表名、动态排序字段）。
  2. **修改风险大**：直接修改生成的代码可能导致后续重新生成时被覆盖，且可能影响现有功能。
  3. **调用方应负责验证**：这些动态参数通常由上层代码传入，合理的架构应该由调用方确保输入的合法性。
- **说明**：这是MyBatis Generator生成的标准代码，`${}`用于需要动态SQL片段的场景。需要确保调用方对输入进行严格验证。
- **建议修复方案**：
  1. 对`orderByClause`使用白名单验证
  2. 审查所有使用`${}`的地方，确保输入可控

---

## 三、安全修复汇总

### 已完成修复统计
| 类别 | 数量 |
|------|------|
| 依赖漏洞 | 7 |
| 配置问题 | 1 |
| 随机数安全 | 1 |
| HTTP安全头 | 1 |
| SQL注入防护 | 2 |
| **总计** | **12** |

### 遗留问题统计
| 类别 | 安全等级 |
|------|----------|
| MD5密码加密 | 🔴 高风险 |
| 配置文件明文密码 | 🟡 中风险 |
| 默认密码过于简单 | 🟡 中风险 |
| lodash原型污染风险 | 🟡 中风险 |
| MyBatis ${}语法风险 | 🟡 中风险 |
| CSRF风险评估 | 🟡 低风险 |

---

## 四、修复建议优先级

### 紧急修复（P0）
1. MD5密码加密升级（需制定详细迁移计划）

### 高优先级（P1）
1. 配置文件敏感信息加密（部署环境配置）
2. 前端lodash依赖迁移
3. 默认密码强度提升

### 中优先级（P2）
1. MyBatis ${}语法输入验证
2. 安全审计日志增强

---

## 五、安全最佳实践建议

### 开发阶段
1. **依赖管理**：定期使用OWASP Dependency-Check、Snyk等工具扫描依赖漏洞
2. **代码审查**：建立安全代码审查流程，重点关注SQL拼接、加密算法、输入验证
3. **安全测试**：集成SAST（静态应用安全测试）工具到CI/CD流程

### 部署阶段
1. **配置安全**：使用环境变量或配置中心管理敏感配置，避免明文存储
2. **HTTPS**：生产环境强制使用HTTPS，配置HSTS响应头
3. **WAF**：考虑部署Web应用防火墙

### 运维阶段
1. **日志审计**：完善安全日志，记录关键操作和异常访问
2. **定期扫描**：定期进行漏洞扫描和渗透测试
3. **应急响应**：建立安全事件应急响应流程

---

## 六、修改文件清单

### 已修改文件
1. `jshERP-boot/pom.xml` - 依赖版本升级
2. `jshERP-boot/src/main/resources/application.properties` - 数据库连接配置
3. `jshERP-boot/src/main/java/com/jsh/erp/config/SecurityConfig.java` - 新建安全配置
4. `jshERP-boot/src/main/java/com/jsh/erp/utils/Tools.java` - 随机数生成器升级
5. `jshERP-boot/src/main/java/com/jsh/erp/utils/QueryUtils.java` - SQL注入修复
6. `jshERP-boot/src/main/java/com/jsh/erp/utils/OrderUtils.java` - SQL注入修复
7. `jshERP-web/package.json` - 前端依赖版本升级

### 需修改文件（遗留问题）
1. `jshERP-boot/src/main/java/com/jsh/erp/utils/Tools.java` - MD5加密升级
2. `jshERP-boot/src/main/java/com/jsh/erp/constants/BusinessConstants.java` - 默认密码
3. `jshERP-web/package.json` - lodash依赖升级
4. `jshERP-web/src/views/user/*.vue` - 密码加密逻辑
5. `jshERP-web/src/views/system/modules/*.vue` - 默认密码硬编码

---

**检查人**：安全审计团队
